import { Card, CardContent } from '@mui/material';
import { Title } from 'react-admin';

function Login() {
    return (
        <Card>
            <Title title="My Page" />
            <CardContent>
                ...
            </CardContent>
        </Card>
    );
}

export default Login;
